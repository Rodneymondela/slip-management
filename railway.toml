[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "flask db upgrade && gunicorn 'sliptrack.app:create_app()' --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
